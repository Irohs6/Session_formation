{% extends 'base.html.twig' %}

{% block title %}Ajouter une Module{% endblock %}

{% block body %}
<div class="container">
    {% if edit %}
        <h1>Modifier une Module</h1>
    {%else%}
     <h1>Ajouter un module</h1>
    {% endif %}
    

    {{form(formModule)}}
    {# {{ form_start(formModule) }}
        
                {{ form_row(formModule.libele) }}
                {{ form_row(formModule.category) }}
          
    {{ form_end(formModule) }} #}
</div>
{% endblock %}
