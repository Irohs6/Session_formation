{% extends 'base.html.twig' %}

{% block title %}Détail d'une session{% endblock %}

{% block body %}

    <div class="container">

        <h1>Fiche d'information satgiaire</h1>
        <a class="btn btn-warning" href="{{ path('edit_stagiaire',{'id':stagiaire.id}) }}">Edit</a>
        <h2>{{ stagiaire }}</h2>

        <p><b>Ville:</b> {{ stagiaire.ville }}</p>
        <p><b>Email:</b> {{ stagiaire.email }}</p>
        <p><b>Tel:</b> {{ stagiaire.tel }}</p>
        <p><b>Date de Naissance:</b> {{ stagiaire.dateNaissance | date('m/d/Y')}}</p>
        <p><b>Genre:</b> {{ stagiaire.genre }}</p>
        <p><b>Age:</b> {{ stagiaire.age }} ans.</p>
        <p><b>Liste des sessions inscrite:</b></p>
        {% for session in stagiaire.sessions %}
            <a class="detailSession" href="{{ path('app_detailSession', {'id': session.id})}}"><p>{{ session }} du {{ session.dateDebut | date('m/d/Y')}} au {{ session.dateFin | date('m/d/Y')}}</p></a>
        {% endfor %} 
        </ul>
    </div>

{% endblock %}